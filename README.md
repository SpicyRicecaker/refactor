## running
```shell
virtualenv venv
# load virtualenv
source venv/bin/activate
# opt1: install dependencies through txt
pip install requirements.txt
# opt2: install dependencies manually
pip install
```